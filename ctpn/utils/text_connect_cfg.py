class Config:
    SCALE=600
    MAX_SCALE=1200
    TEXT_PROPOSALS_WIDTH=16 ### ? 好像没有用上
    MIN_NUM_PROPOSALS = 5
    MIN_RATIO=0.5  ### ? 好像没有用上
    LINE_MIN_SCORE=0.9 ### ? 好像没有用上
    MAX_HORIZONTAL_GAP=16
    TEXT_PROPOSALS_MIN_SCORE=0.8  ### ? 好像没有用上
    TEXT_PROPOSALS_NMS_THRESH=0.3 ### ? 好像没有用上
    MIN_V_OVERLAPS=0.6
    MIN_SIZE_SIM=0.6


